# Optional AGENTS.md Excerpt for Eternity AI Harness Work

When working on Eternity AI-hypothesis or agent-harness tasks:

- Treat this as a scientific validation project, not a demo chatbot.
- Preserve the split between speculative researcher-playground outputs and validated serious-core artifacts.
- New capabilities should come primarily from harness design: memory, evals, tools, provenance, claim auditing, and failure-learning loops.
- Fine-tuning is optional and later-stage; do not assume it is the main solution.
- Every scientific claim must carry provenance and a claim level.
- Synthetic data and model-generated text must never be stored as validated evidence.
- Every failed hypothesis should produce a postmortem lesson or future constraint when possible.
- Prioritize small modules, schemas, deterministic tests, and reports before complex agent orchestration.
- Use latest repo files and Joe’s newest instructions over older strategy documents.
- The project direction is expected to evolve; implement extension points rather than rigid architecture.
